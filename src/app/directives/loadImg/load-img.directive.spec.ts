import { LoadImgDirective } from './load-img.directive';

describe('LoadImgDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadImgDirective();
    expect(directive).toBeTruthy();
  });
});
